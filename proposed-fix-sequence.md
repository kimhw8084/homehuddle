# Proposed first bounded UI0 FIX sequence

Baseline: `46bd155dd55cbd85a29db5722c96d96c4946952b` / tree `a66760b6df143a87e894d5fe636f152ea73f6bea`. This ordering is frozen before the reserved holdout viewport pass. Findings remain provisional until independent Project OS audit.

1. **P1 — Bound the Chores reschedule calendar to the viewport.** Keep the month controls, close action, selected-date summary and `Reschedule Task` action visible at 1440×900 and short heights. Preserve the existing mobile calendar geometry. Root: shared calendar composition/responsive layout. Close with desktop, 1366×768, 1536×864, mobile and landscape captures plus a keyboard path and a chosen-date task oracle. Regression risk: day-cell hit targets and minimum-date navigation.
2. **P1 — Reopen the weekly dinner vote cycle through an ordinary user action.** The current results phase has no route back to voting although `startNextWeek()` already resets the next poll. Add one explicit transition at a meaningful weekly boundary; keep results and scheduling available until that transition is confirmed. Close with no-vote, partial-vote, all-voted, schedule, next-cycle, repeat and stale-state checks. Root: meal-planning state/navigation contract. Regression risk: losing the just-finished results or scheduled menu.
3. **P2 — Make expired wallet rewards non-actionable or resolve them before showing them as active.** In the seeded state, `Sleep In Day` carries an already-past expiration, appears under Active Rewards with an `Expired` label and still opens Use. Close with active, expiring, expired, used and history outcomes for the same object identity. Root: wallet interaction-state/data contract. Regression risk: incorrectly expiring undated rewards or losing notes.
4. **P2 — Align Market fixture identities with actual household identities.** Seeded curator IDs (`Dad`, `Mom`) do not match `Haewon Kim`, `Sarah Kim`, `Leo Kim`, so the ordinary local owner cannot edit/restock several seeded items. Keep role restrictions; bind QA/seed ownership to real deterministic member IDs. Close with owner, non-owner, eligible purchaser and out-of-stock fixtures. Root: fixture/Product contract. Regression risk: granting management actions to the wrong role.
5. **P3 — Remove Reanimated render-time shared-value reads and resolve the opacity/layout warning.** The run emitted repeated warnings while opening the meal result family. First show whether users see jank or stale feedback, then make the smallest component-level correction and retain motion/reduced-motion parity. Root: shared component/animation state. Regression risk: interrupted transitions and gesture cancellation.
6. **P3 — Resolve the nested `settings` route and deprecated web style/animation warnings after task blockers.** Keep the six-tab navigation and existing settings capability. Close with direct/deep-link/Back behavior and browser console evidence. Root: navigation/platform adapter. Regression risk: route back-stack changes.

Protect during convergence: the six-destination mental model, Home's dense household digest and utility widgets, Chores' scanable categories and high-frequency assignment controls, Family's member/heatmap/recent-activity capability, Wallet's balance history and reward bag, Market's sale/redeem flows, and Restock's category organization, quick add and completed-item history. Do not flatten or remove these workflows to reduce surface count.
